<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SecondProject" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SecondProject" parent="Base.Theme.SecondProject" />

    <style name="CustomBottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconSize">24dp</item>
        <item name="itemTextAppearanceActive">@style/BottomNavTextStyle</item>
        <item name="itemTextAppearanceInactive">@style/BottomNavTextStyle</item>

        <item name="itemBackground">@drawable/buttonitemdrawable</item>
        <item name="itemRippleColor">@color/black</item>
        <item name="itemHorizontalPadding">8dp</item>
        <item name="itemPaddingBottom">8dp</item>
    </style>

    <style name="BottomNavTextStyle" parent="TextAppearance.AppCompat.Large">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/blues</item>
    </style>

    <style name="Ohter" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="background">@drawable/otherone</item>
    </style>

    <style name="CustomButtonStyle" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@drawable/otherone</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@drawable/otherone</item>
    </style>

</resources>